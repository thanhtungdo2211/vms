camera_api:
  host: "${API_HOST:0.0.0.0}"
  port: "${API_PORT:8083}"

pipeline:
  name: multi-branch-demux-pipeline
  branches:
    # Recognition with demux for per-camera annotated streams
    recognition: apps/face/config-demux.yaml
    # Detection with demux for per-camera annotated streams
    detection: apps/detection/config-demux.yaml
